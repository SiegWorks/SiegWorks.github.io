{
  "name": "voicon-license-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Voicon License API (Cloudflare Workers)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "check": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.114.0"
  }
}
